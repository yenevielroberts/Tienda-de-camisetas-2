# Puerto y entorno
PORT=3000
NODE_ENV=development

# DATABASE_URL - Elige una de las opciones y copia a .env (quita la que no uses)
# Opción PostgreSQL (producción / staging):
# DATABASE_URL="postgresql://usuario:password@localhost:5432/tienda?schema=public"

# Opción SQLite (rápido para desarrollo local) - recomendado para empezar
DATABASE_URL="file:./dev.db"

# Clave secreta para firmar JWT (cámbiala en producción)
JWT_SECRET=change_this_secret

# URL del frontend (para CORS, redirecciones, etc.)
FRONTEND_URL=http://localhost:5173

# Notas:
# - Copia este fichero a .env y ajusta valores antes de ejecutar migraciones o levantar el servidor.
# - Si usas PostgreSQL asegúrate de que la base de datos existe y las credenciales son correctas.